{
    "Lua.diagnostics.globals": [
        "cmake",
    ],
    "Lua.workspace.ignoreDir": [
        ".vscode",
        "bin",
    ],
    "Lua.runtime.path": [
        "lua/?.lua",
        
        // We only add '.so' since lua queries for the right dynamic library it self
        "lib/?.so"
    ]
}
